# One Tool to Rule Them All — Mac

Native Swift/SwiftUI rewrite of [One Tool to Rule Them All](https://github.com/Brightwav3/One-tool-to-rule-them-all), the local-first file converter.

## Run

```bash
swift run OneTool
```

Open `Package.swift` in Xcode to develop with previews.
